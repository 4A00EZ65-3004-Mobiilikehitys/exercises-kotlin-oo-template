package exercise

import org.apache.logging.log4j.LogManager

/**
 * The main function of the application.
 *
 * It attempts to parse a single integer command-line argument specifying the number of [Person] objects to generate.
 * If successful, it generates two lists: one with the specified number of random [Person] objects and another
 * with distinct elements from the first list. Both lists and a comparison of their sizes are logged.
 *
 * @param args The command-line arguments passed to the program.
 */
fun main(args: Array<String>) {
    val logger = LogManager.getLogger()

}
